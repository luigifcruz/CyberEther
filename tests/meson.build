if catch2_dep.found()
    subdir('memory')
    subdir('platform')
    subdir('parser')
    subdir('settings')
    subdir('modules')
    subdir('flowgraph')
    subdir('render')
endif

subdir('components')
