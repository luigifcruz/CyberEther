# CyberEther Platform Apps

This directory contains platform-specific app projects and packaging metadata.

- `android/` and `ios/` contain mobile app projects.
- `linux/`, `windows/`, and `macos/` contain desktop release packaging assets for the cross-platform `main.cc` executable.
